def filter_schools(transfer_credits):
    unique_schools = set()
    filtered_schools = []
    
    for record in transfer_credits:
        school = record['school']
        if school not in unique_schools:
            unique_schools.add(school)
            filtered_schools.append(record)
            
    return filtered_schools
